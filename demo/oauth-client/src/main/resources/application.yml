server:
  port: 1003
spring:
  application:
    name: oauth-client
  thymeleaf:
    #修改跳转的前缀和后缀,以下注释部分是spring boot的默认配置
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    encoding: UTF-8
    #开发过程中需将缓存关闭
    cache: false
    check-template: false